<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Deliberately not sandboxed. Wiley's whole job is running an agent that edits
  files and runs bash across a workspace the user chose, and spawning the
  claude and codex CLIs already installed on the machine. App Sandbox permits
  none of that, so the honest posture is the hardened runtime without the
  sandbox rather than a sandbox with a pile of temporary exceptions.
-->
<plist version="1.0">
  <dict>
    <!-- V8 compiles JavaScript at runtime. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Workers inherit PATH and other variables from the app. -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- The agent SDK loads native modules the app did not sign. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- The point of the product: talking to it. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>
