node_modules/
out/
release/
coverage/
.env
.wiley/
# Pre-rename workspace data. Kept for one release so an existing checkout does
# not start reporting its own board as untracked.
.board-ai/
.DS_Store
*.log
test-results/
# Intermediate for build/icon.icns; scripts/make-icon.sh regenerates both.
build/icon.iconset/
playwright-report/
.e2e/
# User-supplied music track; copyrighted audio never gets committed.
src/renderer/assets/wearing-my-rolex.*

# Agent-generated demo output
site/

# Scratch design files that were never meant to ship with the source
Plan.*
